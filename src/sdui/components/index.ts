// All SDUI components are now imported directly by path in
// componentRegistry.ts (e.g. @sdui/components/HeaderSearch).
// This barrel is intentionally empty — named exports only when
// needed, per AGENTS.md (never `export *`).
export {};